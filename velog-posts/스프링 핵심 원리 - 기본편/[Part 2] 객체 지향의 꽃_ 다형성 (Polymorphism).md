# [Part 2] 객체 지향의 꽃: 다형성 (Polymorphism)

**Published:** 2026-01-14T07:35:09.796Z
**Link:** https://velog.io/@kik328288/Part-2-객체-지향의-꽃-다형성-Polymorphism

---

### 1. 좋은 객체 지향 프로그래밍이란?

---

스프링은 **"좋은 객체 지향 애플리케이션을 개발할 수 있게 돕는 프레임워크"**입니다. 그렇다면 좋은 객체 지향이란 무엇일까요?

### 1) 객체 지향의 정의

---

객체 지향 프로그래밍은 컴퓨터 프로그램을 단순히 명령어의 목록으로 보는 시각에서 벗어나, 여러 개의 독립된 단위, 즉 **"객체"들의 모임**으로 파악하고자 하는 것입니다.

- **협력(Collaboration):** 각각의 객체는 메시지를 주고받고, 데이터를 처리할 수 있습니다.
- **유연성과 변경 용이성:** 프로그램을 유연하고 변경이 용이하게 만들기 때문에 대규모 소프트웨어 개발에 많이 사용됩니다.

### 2) "유연하고 변경이 용이하다"는 뜻은?

---

마치 **레고 블럭**을 조립하듯이, **키보드와 마우스**를 갈아 끼우듯이, **컴퓨터 부품**을 교체하듯이 컴포넌트를 쉽고 유연하게 변경하면서 개발할 수 있다는 뜻입니다.

이것을 가능하게 하는 핵심 특징이 바로 **다형성(Polymorphism)**입니다.

### 2. 다형성의 실세계 비유 (역할과 구현의 분리)

---

다형성은 설명하기 추상적이지만, 실세계의 비유를 들면 이해하기 쉽습니다. 핵심은 **세상을 '역할(Role)'과 '구현(Implementation)'으로 구분**하는 것입니다.

💡 예시 1: 운전자와 자동차

- **역할:** 운전자, 자동차.
- **구현:** K3, 아반떼, 테슬라 모델3.

**운전자(클라이언트) 관점에서의 장점:**

1. 운전자는 **자동차의 역할(인터페이스)**만 알면 됩니다.
2. 운전자는 **자동차의 내부 구조(구현)**를 몰라도 운전할 수 있습니다.
3. 자동차가 **내부적으로 구조가 바뀌어도(엔진 교체 등)** 운전자는 영향을 받지 않습니다.
4. 심지어 **자동차 대상을 다른 차종(구현체)으로 변경**해도 운전자는 새로 운전을 배울 필요가 없습니다 (유연함).

![](https://velog.velcdn.com/images/kik328288/post/510c1fee-9084-4dc6-a82c-07f566c89990/image.png)

💡 예시 2: 로미오와 줄리엣 공연

- **역할:** 로미오, 줄리엣.
- **구현(배우):** 장동건, 원빈, 김태희, 송혜교.

로미오 역할을 맡은 장동건(배우)이 사정이 생겨서 원빈으로 바뀐다고 해도, 줄리엣(상대 배우)은 공연을 하는 데 문제가 없습니다. 즉, **대상의 역할만 정해져 있다면 구현체(배우)는 언제든지 대체 가능**합니다.

![](https://velog.velcdn.com/images/kik328288/post/5b419c42-d08a-418d-8a77-c419408f420b/image.png)

### 3. 역할과 구현을 분리했을 때의 장점

---

이 개념을 프로그래밍(객체 세상)으로 가져오면 다음과 같습니다.

- **역할** = 인터페이스 (Interface)
- **구현** = 인터페이스를 구현한 클래스, 구현 객체

핵심 장점 (클라이언트 기준)

1. 클라이언트는 대상의 **역할(인터페이스)만 알면** 됩니다.
2. 클라이언트는 구현 대상의 **내부 구조를 몰라도** 됩니다.
3. 클라이언트는 구현 대상의 **내부 구조가 변경되어도** 영향을 받지 않습니다.
4. 클라이언트는 **구현 대상 자체를 변경해도** 영향을 받지 않습니다.

**결론:** 객체 설계 시 **역할(인터페이스)을 먼저 부여**하고, 그 역할을 수행하는 구현 객체를 만드는 것이 순서입니다.

### 4. 자바 언어에서의 다형성 (협력 관계)

---

자바에서는 **오버라이딩(Overriding)**을 통해 다형성을 구현합니다.

### 1) 다형성 코드의 구조

---

- **Interface:** `MemberRepository` (save() 메서드 보유)
- **Implementation:** `MemoryMemberRepository`, `JdbcMemberRepository`
- **Client:** `MemberService`

### 2) 실행 시점의 유연성

---

`MemberService`(클라이언트)는 `MemberRepository`(인터페이스)를 의존합니다.

```java
// MemberService 코드의 일부
private MemberRepository memberRepository = new MemoryMemberRepository(); 
// 또는
private MemberRepository memberRepository = new JdbcMemberRepository();
```

오버라이딩된 메서드는 실행 시점에 실제 생성된 객체의 메서드가 실행됩니다. 즉, 클라이언트는 `save()`를 호출하지만, 실제로는 `Memory`의 save가 호출될 수도, `Jdbc`의 save가 호출될 수도 있습니다.

> 다형성의 본질:인터페이스를 구현한 객체 인스턴스를 실행 시점에 유연하게 변경할 수 있습니다.
클라이언트를 변경하지 않고, 서버의 구현 기능을 유연하게 변경할 수 있는 것이 핵심입니다.
> 

![](https://velog.velcdn.com/images/kik328288/post/2d842d3f-27e1-42f3-9d77-d8428ca71a19/image.png)

![](https://velog.velcdn.com/images/kik328288/post/be23a83a-6f54-41b2-afea-3fb7461ea053/image.png)

### 5. 역할과 구현 분리의 정리 및 한계

---

### 1) 역할과 구현 분리의 정리

---

- 실세계의 역할과 구현이라는 편리한 컨셉을 다형성을 통해 객체 세상으로 가져올 수 있습니다.
- 유연하고, 변경이 용이하며 확장 가능한 설계를 가지고 있습니다.
- 클라이언트에 영향을 주지 않는 변경이 가능합니다.

### 2) 역할과 구현 분리의 한계

---

역할과 구현의 분리는 무척 훌륭하지만, 치명적인 한계가 하나 존재합니다.

- **문제점:** **역할(인터페이스) 자체가 변하면**, 클라이언트와 서버 모두에 엄청난 변경이 발생합니다.
    - 예) 자동차를 비행기로 변경해야 한다면?
    - 예) USB 인터페이스 규격 자체가 바뀐다면?
    - 예) 대본 자체가 완전히 바뀐다면?

**결론:** 그렇기 때문에 **인터페이스를 안정적으로 잘 설계하는 것**이 가장 중요합니다.

### 6. 스프링과 객체 지향

---

그래서 스프링은 이 **다형성(Polymorphism)**을 극대화해서 이용할 수 있게 도와줍니다.

- 스프링의 **제어의 역전(IoC)**, **의존관계 주입(DI)**은 결국 다형성을 활용해서 역할과 구현을 편리하게 다룰 수 있도록 지원하는 기능입니다.
- 스프링을 사용하면 마치 레고 블럭 조립하듯이, 공연 무대의 배우를 선택하듯이 구현을 편리하게 변경할 수 있습니다.

